export interface substage {
    expected_end_date: string,
    id: number,
    name: string,
    duration: number,
    description: string,
    start_date: string,
    status: string,
    createdAt: string,
    updatedAt: string,
    stageId: number
}